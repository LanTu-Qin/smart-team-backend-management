# 生产构建（vite build 默认 mode=production）→ mock（演示口径）
# ----------------------------------------------------------------------------
# 【当前口径】
#     npm run dev            → .env                  → mock
#     npm run build          → .env + 本文件          → mock（默认产物，可对外演示，不碰真库）
#     npm run dev:cloud      → .env + .env.cloud      → 真后端（本地调真数据）
#     npm run build:cloud    → .env + .env.cloud      → 真后端（出真数据产物就用这条）
#
# ⚠️ 真后端产物必须显式用 `npm run build:cloud`：默认 build 出来的是 mock 版。
#    部署前请先确认页面页脚：显示「演示环境…」= mock，「已接入微信云开发真实环境…」= 真后端。
#
# ⚠️ Vite 的环境变量是构建期替换：改完必须重新 build（本地）/ 重新"更新服务"（平台），运行时改无效。
# ⚠️ 环境 ID / 地域不是密钥（必然出现在前端产物里）；真正的密钥只在云函数环境变量里。

VITE_API_MODE=mock
# VITE_API_MODE=cloud
VITE_CLOUD_ENV=cloud1-d8gb9nir3847ec081
VITE_CLOUD_REGION=ap-shanghai
