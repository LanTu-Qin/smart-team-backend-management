# 生产构建（vite build 默认 mode=production）→ 真后端
# ----------------------------------------------------------------------------
# 【为什么要单独有这一个文件】踩过的坑：
#   `npm run build` 的默认 mode 是 production，而之前只有 `.env`（VITE_API_MODE=mock）
#   会被加载 → 本地 build 与平台 CI 构建出来的都是 **mock 版**，
#   现象就是"代码明明接了真后端，部署上去还是模拟数据"，而且看日志毫无异常。
#
#   现在把口径定死：**开发 = mock，生产构建 = 真后端**。
#     npm run dev            → .env                  → mock（不依赖云环境，随便点）
#     npm run build          → .env + .env.production → 真后端（本文件）
#     npm run dev:cloud      → .env + .env.cloud      → 真后端（本地调真数据用）
#
# ⚠️ Vite 的环境变量是构建期替换：改完必须重新 build（本地）/ 重新"更新服务"（平台），运行时改无效。
# ⚠️ 环境 ID / 地域不是密钥（必然出现在前端产物里）；真正的密钥只在云函数环境变量里。


VITE_API_MODE=mock
# VITE_API_MODE=cloud
VITE_CLOUD_ENV=cloud1-d8gb9nir3847ec081
VITE_CLOUD_REGION=ap-shanghai
