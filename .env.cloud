# 真后端模式（npm run dev:cloud / build:cloud）
# 通道：CloudBase Web SDK（匿名登录做传输）+ 自签 adminToken（做鉴权），见 docs/api.md 第 2 节。
#
# ⚠️ Vite 的环境变量是**构建期替换**：改了这几个值必须重启 dev 或重新 build，运行时改无效。
# ⚠️ 环境 ID / 地域不是密钥（它们会出现在前端产物里），所以可以放心提交；
#    真正的密钥（ADMIN_TOKEN_SECRET / AI_API_KEY）只存在云函数环境变量里，永远不进前端。
VITE_API_MODE=cloud
VITE_CLOUD_ENV=cloud1-d8gb9nir3847ec081
VITE_CLOUD_REGION=ap-shanghai
