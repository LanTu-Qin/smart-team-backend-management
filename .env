# 开发模式（npm run dev）→ Tier-1 mock
# 为什么开发默认 mock：本地不依赖云环境、不怕手滑改到生产库、演示站也能随时打开。
# 生产构建（npm run build）同样加载 `.env.production`，那里也是 mock —— 即**默认产物就是 mock 版**。
# 要出真后端产物：`npm run build:cloud`（读 `.env.cloud`，VITE_API_MODE=cloud）。
VITE_API_MODE=mock
