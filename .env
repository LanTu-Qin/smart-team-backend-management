# 开发模式（npm run dev）→ Tier-1 mock
# 为什么开发默认 mock：本地不依赖云环境、不怕手滑改到生产库、演示站也能随时打开。
# ⚠️ 生产构建**不读这个文件的价值取向**：vite build 的 mode=production 会再加载
#    `.env.production`（VITE_API_MODE=cloud）覆盖它 —— 见那个文件里的说明。
VITE_API_MODE=mock
